---
import { DEFAULT_LOCALE, type Locale } from '../i18n/locales'
import { siteConfig } from '../config'

interface Props {
  locale?: Locale
}

const { locale = DEFAULT_LOCALE } = Astro.props
---

<div id="config-carrier" data-hue={siteConfig.themeColor.hue} data-locale={locale}></div>
